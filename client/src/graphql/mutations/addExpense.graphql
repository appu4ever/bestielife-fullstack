mutation CreateExpense($options: CreateExpenseInput!) {
  createExpense(options: $options) {
    errors {
      field
      message
    }
    expense {
      amount
      date
      description
      createdAt
      updatedAt
    }
  }
}
